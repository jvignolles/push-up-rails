- content_for :sidebar do
  = render "search_form"

- url = [[@__namespaces], @item].flatten.reject(&:blank?)
= simple_form_for url, html: { class: simple_form_classes, autocomplete: "off" } do |f|
  = render f
  = submit_button f

